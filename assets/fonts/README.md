# Bundled social typography fonts

Place the production font files here so preview and export use the same Qt-loaded font family:

- `Montserrat-ExtraBold.ttf` (primary)
- `Poppins-ExtraBold.ttf` (fallback)

`SocialTypographyRenderer` loads these files before falling back to installed Qt fonts.
